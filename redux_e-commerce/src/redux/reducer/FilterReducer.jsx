const initialState = {
    _page: 1,
    _limit: 16,
    _sort: "",
    type: [],
    brand: [],
    q: ""
}

const FilterReducer = (state = initialState, action) => {
    return state
}

export default FilterReducer
